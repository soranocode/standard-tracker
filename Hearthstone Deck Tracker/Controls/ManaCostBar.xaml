<UserControl x:Class="Hearthstone_Deck_Tracker.ManaCostBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hearthstoneDeckTracker="clr-namespace:Hearthstone_Deck_Tracker"
             xmlns:local="clr-namespace:Hearthstone_Deck_Tracker.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Background="Transparent"
             BorderBrush="Transparent" BorderThickness="0">
    <UserControl.ToolTip>
        <local:ManaCostBarToolTip x:Name="McbToolTip" />
    </UserControl.ToolTip>
    <UserControl.Resources>
        <Style x:Key="{x:Type ToolTip}" TargetType="{x:Type ToolTip}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Bottom" Margin="2,1,2,0">
            <Rectangle Name="WeaponsRect" Height="0" RadiusX="3" RadiusY="3" StrokeThickness="0">
                <Rectangle.Fill>
                    <SolidColorBrush Color="#79B4FF"/>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle Name="SpellsRect" Height="0" RadiusX="3" RadiusY="3" StrokeThickness="0">
                <Rectangle.Fill>
                    <SolidColorBrush Color="#79B4FF"/>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle Name="MinionsRect" Height="0" RadiusX="3" RadiusY="3" StrokeThickness="0">
                <Rectangle.Fill>
                    <SolidColorBrush Color="#79B4FF"/>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle Name="HeroesRect" Height="0" RadiusX="3" RadiusY="3" StrokeThickness="0">
                <Rectangle.Fill>
                    <SolidColorBrush Color="#79B4FF"/>
                </Rectangle.Fill>
            </Rectangle>
        </StackPanel>
        <TextBlock Name="TextBlockCount" FontWeight="SemiBold" Text= "0" Height="18" Foreground="#A3B0C4"
               VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="11"/>

    </Grid>
</UserControl>
